<%= form_for @menu, :html => { :class => "form-horizontal menu" } do |f| %>

    <% if @menu.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@menu.errors.count, "error") %> prohibited this menu from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @menu.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@menu[:name]) %>
  </div>
  <div class="control-group">
    <%= f.label :tpe, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :tpe, :class => 'form-control' %>
    </div>
    <%= error_span(@menu[:tpe]) %>
  </div>
  <div class="control-group">
    <%= f.label :price, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :price, :class => 'form-control' %>
    </div>
    <%= error_span(@menu[:price]) %>
  </div>
  <div class="control-group">
    <%= f.label :order_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :order_id, :class => 'form-control' %>
    </div>
    <%= error_span(@menu[:order_id]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            menus_path, :class => 'btn btn-default' %>

<% end %>
